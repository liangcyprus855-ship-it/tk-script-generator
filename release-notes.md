# 1.0.20

- 每次生成成功或失败都会保存到本地生成历史索引。
- 应用启动和登录后先读取本地历史，再用服务器记录补齐，网络暂时不可用时仍可查看。
- 本地记录按用户账号隔离，保存脚本内容、时间、时长、金额、产品和状态。

# 1.0.19

- 复制当前脚本和历史记录时，只保留脚本标题、Hook、分镜、旁白与 CTA。
- 移除复制内容中的生成时长和消耗金额字段。

# 1.0.18

- 主内容区扩大至最高 1480px，左右采用 56:44 比例。
- 左侧配置紧凑堆叠，默认显示摘要，展开后分类并排、上传区缩小。
- 右侧改为脚本阅读器，支持脚本 01 / 02 / 03 切换。
- 统一标题、Hook、时间轴、镜头与旁白层级。
- 左右独立滚动，费用与生成按钮固定在结果面板底部。
