TK 跨境带货脚本生成器 1.0.16

- 生成改为由服务器统一校验账户余额、调用 MiMo-V2.5 并保存内容与消费记录。
- 余额不足时，生成按钮立即禁用并显示所需金额；服务器再次校验，防止余额透支。
- 提交后显示生成状态，重复提交不会重复扣费；失败或服务器任务中断自动退回费用。
- 网络暂时断开、重新打开应用后，自动恢复查询最近生成任务。
- 商业客户端不再读取本地模型密钥，也不再依赖本地与云端两套登录状态。

需要升级到本版本才能使用新的服务器生成接口。单次收费标准保持不变，10 秒 ¥0.20、15 秒 ¥0.30、20–30 秒 ¥0.60、45 秒 ¥0.90、60 秒 ¥1.20，每次生成三款脚本。
