const json = (data, status = 200) => new Response(JSON.stringify(data), { status, headers: { "content-type": "application/json; charset=utf-8", "access-control-allow-origin": "*", "access-control-allow-headers": "content-type,authorization,wechatpay-signature,wechatpay-timestamp,wechatpay-nonce,wechatpay-serial", "access-control-allow-methods": "GET,POST,OPTIONS" } });
const text = (value) => String(value ?? "").trim();
const now = () => new Date().toISOString();
const encoder = new TextEncoder();
const hex = (bytes) => [...new Uint8Array(bytes)].map((b) => b.toString(16).padStart(2, "0")).join("");
const b64 = (bytes) => btoa(String.fromCharCode(...new Uint8Array(bytes)));
const fromB64 = (value) => Uint8Array.from(atob(value), (c) => c.charCodeAt(0));

async function sha256(value) { return hex(await crypto.subtle.digest("SHA-256", encoder.encode(value))); }
async function passwordHash(password, salt = crypto.randomUUID(), iterations = 100000) {
  const key = await crypto.subtle.importKey("raw", encoder.encode(password), "PBKDF2", false, ["deriveBits"]);
  const bits = await crypto.subtle.deriveBits({ name: "PBKDF2", salt: encoder.encode(salt), iterations, hash: "SHA-256" }, key, 256);
  return `${salt}:${hex(bits)}`;
}
async function checkPassword(password, stored) {
  const [salt, expected] = text(stored).split(":");
  if (!salt || !expected) return false;
  for (const iterations of [100000, 120000]) {
    try {
      const actual = await passwordHash(password, salt, iterations);
      if (actual === `${salt}:${expected}`) return true;
    } catch {}
  }
  return false;
}
function authToken(request) { return text(request.headers.get("authorization")).replace(/^Bearer\s+/i, ""); }
async function isAdmin(request, env) { const supplied = text(request.headers.get("x-admin-token") || authToken(request)); if (text(env.ADMIN_TOKEN) && supplied === text(env.ADMIN_TOKEN)) return true; const user = await userFrom(request, env); return Boolean(user && text(env.ADMIN_EMAIL) && user.email === text(env.ADMIN_EMAIL).toLowerCase()); }
async function ensureBalanceColumn(env) { try { await env.DB.prepare("ALTER TABLE accounts ADD COLUMN balance_fen INTEGER").run(); } catch {} await env.DB.prepare("UPDATE accounts SET balance_fen=credits*10 WHERE balance_fen IS NULL").run(); }
async function ensureAccountAdminColumns(env) { try { await env.DB.prepare("ALTER TABLE accounts ADD COLUMN admin_note TEXT").run(); } catch {} try { await env.DB.prepare("ALTER TABLE accounts ADD COLUMN membership TEXT").run(); } catch {} }
async function userFrom(request, env) {
  await ensureBalanceColumn(env);
  const token = await sha256(authToken(request));
  if (!token) return null;
  return env.DB.prepare("SELECT a.id, a.email, a.balance_fen FROM sessions s JOIN accounts a ON a.id=s.account_id WHERE s.token_hash=? AND s.expires_at>?").bind(token, now()).first();
}
async function requireUser(request, env) { return userFrom(request, env); }
function packages(id) { return ({ starter: [990, 100], creator: [3990, 500], studio: [9990, 1500] })[id] || null; }
function publicUser(row) { const balanceFen = Number(row?.balance_fen ?? Number(row?.credits || 0) * 10); return { id: row.id, email: row.email, balanceFen, balanceYuan: (balanceFen / 100).toFixed(2) }; }
function pemToBytes(pem) { return fromB64(pem.replace(/-----[^-]+-----/g, "").replace(/\s+/g, "")); }
async function rsaKey(pem, usage) { return crypto.subtle.importKey("pkcs8", pemToBytes(pem), { name: "RSASSA-PKCS1-v1_5", hash: "SHA-256" }, false, usage); }
async function rsaPublicKey(pem) { return crypto.subtle.importKey("spki", pemToBytes(pem), { name: "RSASSA-PKCS1-v1_5", hash: "SHA-256" }, false, ["verify"]); }
async function verifyWechat(request, rawBody, env) {
  const signature = text(request.headers.get("wechatpay-signature")); const timestamp = text(request.headers.get("wechatpay-timestamp")); const nonce = text(request.headers.get("wechatpay-nonce"));
  if (!signature || !timestamp || !nonce || !text(env.WECHAT_PLATFORM_PUBLIC_KEY)) return false;
  const key = await rsaPublicKey(env.WECHAT_PLATFORM_PUBLIC_KEY);
  return crypto.subtle.verify("RSASSA-PKCS1-v1_5", key, fromB64(signature), encoder.encode(`${timestamp}\n${nonce}\n${rawBody}\n`));
}
async function wechatNative(env, order) {
  const required = ["WECHAT_APP_ID", "WECHAT_MCH_ID", "WECHAT_SERIAL_NO", "WECHAT_API_V3_KEY", "WECHAT_PRIVATE_KEY", "WECHAT_NOTIFY_URL"];
  const missing = required.filter((key) => !text(env[key]));
  if (missing.length) throw Object.assign(new Error(`微信支付配置不完整：${missing.join("、")}`), { status: 503 });
  if (new TextEncoder().encode(env.WECHAT_API_V3_KEY).length !== 32) throw Object.assign(new Error("WECHAT_API_V3_KEY 必须为 32 字节"), { status: 503 });
  const path = "/v3/pay/transactions/native";
  const body = JSON.stringify({ appid: env.WECHAT_APP_ID, mchid: env.WECHAT_MCH_ID, description: `TK脚本生成器-${order.package_id}`, out_trade_no: order.id, notify_url: env.WECHAT_NOTIFY_URL, amount: { total: order.amount_fen, currency: "CNY" } });
  const timestamp = String(Math.floor(Date.now() / 1000)); const nonce = crypto.randomUUID().replaceAll("-", "");
  const key = await rsaKey(env.WECHAT_PRIVATE_KEY, ["sign"]);
  const signature = b64(await crypto.subtle.sign("RSASSA-PKCS1-v1_5", key, encoder.encode(`POST\n${path}\n${timestamp}\n${nonce}\n${body}\n`)));
  const response = await fetch(`${env.WECHAT_API_BASE_URL || "https://api.mch.weixin.qq.com"}${path}`, { method: "POST", headers: { "content-type": "application/json", authorization: `WECHATPAY2-SHA256-RSA2048 mchid="${env.WECHAT_MCH_ID}",nonce_str="${nonce}",timestamp="${timestamp}",serial_no="${env.WECHAT_SERIAL_NO}",signature="${signature}"` }, body });
  const result = await response.json().catch(() => ({}));
  if (!response.ok || !result.code_url) throw Object.assign(new Error(`微信统一下单失败：${text(result.message) || response.status}`), { status: 502 });
  return result.code_url;
}
async function decryptWechat(resource, key) {
  const encrypted = fromB64(resource.ciphertext); const data = encrypted.slice(0, -16); const tag = encrypted.slice(-16);
  const cryptoKey = await crypto.subtle.importKey("raw", encoder.encode(key), "AES-GCM", false, ["decrypt"]);
  const plain = await crypto.subtle.decrypt({ name: "AES-GCM", iv: encoder.encode(resource.nonce), additionalData: encoder.encode(resource.associated_data || "",) }, cryptoKey, new Uint8Array([...data, ...tag]));
  return JSON.parse(new TextDecoder().decode(plain));
}
async function ensureProofTable(env) { await env.DB.prepare("CREATE TABLE IF NOT EXISTS payment_proofs (order_id TEXT PRIMARY KEY, note TEXT NOT NULL, created_at TEXT NOT NULL)").run(); }
async function ensureLedgerReference(env) { try { await env.DB.prepare("ALTER TABLE ledger ADD COLUMN reference TEXT").run(); } catch {} }
// The admin page talks to the isolated LA-02 PostgreSQL API over HTTPS.
const adminPage = `<!doctype html><meta charset="utf-8"><title>TK脚本生成器后台</title><style>body{font-family:system-ui;margin:32px;background:#f6f7fb;color:#172033}button,input,select{padding:8px 12px;margin:4px}table{border-collapse:collapse;background:white;width:100%}td,th{padding:10px;border:1px solid #ddd;text-align:left;font-size:13px}.hidden{display:none}.error{color:#c92a2a}.tabs{margin:14px 0}h2{font-size:18px;margin:16px 0 8px}.small{font-size:12px;color:#667085}</style><h1>TK脚本生成器 · 管理后台</h1><section id="loginBox"><p>管理员邮箱：<input id="email" type="email" value="253874384@qq.com"></p><p>管理员密码：<input id="password" type="password"><button id="loginButton" onclick="login()">登录后台</button></p><div id="loginMsg" class="error"></div></section><section id="panel" class="hidden"><div class="tabs"><button onclick="show('users')">用户</button><button onclick="show('orders')">充值审核</button><button onclick="loadAll()">刷新</button></div><div id="msg"></div><div id="users"><h2>用户</h2><p class="small">显示所有已经注册的用户，可添加备注、调整会员级别或单独充值。</p><table><thead><tr><th>注册时间</th><th>邮箱</th><th>余额</th><th>生成次数</th><th>备注</th><th>会员级别</th><th>单独充值（元）</th><th>操作</th></tr></thead><tbody id="userRows"></tbody></table></div><div id="orders" class="hidden"><h2>待审核充值</h2><table><thead><tr><th>时间</th><th>订单</th><th>账户</th><th>充值金额</th><th>备注</th><th>到账金额（分）</th><th>操作</th></tr></thead><tbody id="orderRows"></tbody></table><h2>充值记录</h2><table><thead><tr><th>时间</th><th>用户</th><th>金额</th><th>充值方式</th><th>状态</th><th>说明</th></tr></thead><tbody id="historyRows"></tbody></table></div></section><script>let auth='';const el=id=>document.getElementById(id);const fmt=t=>{const d=new Date(t);return isNaN(d)?t:d.toLocaleString('zh-CN',{hour12:false})};const channel=x=>x==='alipay_personal'?'支付宝支付':x==='wechat'?'微信支付':x==='admin'?'管理员充值':x==='mock'?'测试充值':x;const status=x=>x==='paid'?'已付款':x==='proof_submitted'?'待审核':x==='rejected'?'未生效':x==='pending'?'未付款':x==='manual'?'已生效':x;async function login(){const b=el('loginButton'),m=el('loginMsg');b.disabled=true;m.textContent='登录中…';try{const r=await fetch('/api/account/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:el('email').value.trim(),password:el('password').value})});const d=await r.json().catch(()=>({}));if(!r.ok){m.textContent=d.error||('登录失败（HTTP '+r.status+'）');return}auth=d.token;el('loginBox').classList.add('hidden');el('panel').classList.remove('hidden');await loadAll()}catch(e){m.textContent='网络错误：'+e.message}finally{b.disabled=false}}async function loadAll(){await loadUsers();await loadOrders()}async function loadUsers(){const r=await fetch('/api/admin/users',{headers:{authorization:'Bearer '+auth}});const d=await r.json().catch(()=>({}));if(!r.ok){el('msg').textContent=d.error||'用户加载失败';return}el('userRows').innerHTML=d.users.map(u=>'<tr><td>'+fmt(u.created_at)+'</td><td>'+u.email+'</td><td>¥'+u.balance_yuan+'</td><td>'+u.generation_count+'</td><td><input id="note-'+u.id+'" value="'+((u.admin_note||'')+'').replaceAll("'",'')+'" placeholder="备注"></td><td><select id="mem-'+u.id+'"><option '+(u.membership==='普通用户'?'selected':'')+'>普通用户</option><option '+(u.membership==='月度会员'?'selected':'')+'>月度会员</option><option '+(u.membership==='年度会员'?'selected':'')+'>年度会员</option></select></td><td><input id="recharge-'+u.id+'" type="number" min="0.01" step="0.01" placeholder="元" style="width:70px"></td><td><button data-user-id="'+u.id+'" onclick="saveUser(this.dataset.userId)">保存</button><button data-user-id="'+u.id+'" onclick="rechargeUser(this.dataset.userId)">充值</button></td></tr>').join('')}async function saveUser(id){const r=await fetch('/api/admin/users/'+id,{method:'PATCH',headers:{authorization:'Bearer '+auth,'content-type':'application/json'},body:JSON.stringify({note:el('note-'+id).value,membership:el('mem-'+id).value})});const d=await r.json();el('msg').textContent=d.error||'用户资料已保存';loadUsers()}async function rechargeUser(id){const yuan=Number(el('recharge-'+id).value);if(!Number.isFinite(yuan)||yuan<=0){el('msg').textContent='请输入正确的充值金额';return}const r=await fetch('/api/admin/users/'+id+'/recharge',{method:'POST',headers:{authorization:'Bearer '+auth,'content-type':'application/json'},body:JSON.stringify({amountFen:Math.round(yuan*100)})});const d=await r.json();el('msg').textContent=d.error||d.message||'充值完成';loadAll()}async function loadOrders(){const [a,b]=await Promise.all([fetch('/api/admin/orders',{headers:{authorization:'Bearer '+auth}}),fetch('/api/admin/orders/history',{headers:{authorization:'Bearer '+auth}})]);const d=await a.json().catch(()=>({})),h=await b.json().catch(()=>({}));if(!a.ok||!b.ok){el('msg').textContent=d.error||h.error||'订单加载失败';return}el('historyRows').innerHTML=(h.orders||[]).map(o=>'<tr><td>'+fmt(o.created_at)+'</td><td>'+o.email+'</td><td>¥'+(Number(o.amount_fen||0)/100).toFixed(2)+'</td><td>'+channel(o.provider)+'</td><td>'+status(o.status)+'</td><td>'+((o.description||o.note||'')+'').replaceAll('<','&lt;')+'</td></tr>').join('');el('orderRows').innerHTML=d.orders.map(o=>'<tr><td>'+fmt(o.created_at)+'</td><td>'+o.id+'</td><td>'+o.email+'</td><td>¥'+(o.amount_fen/100).toFixed(2)+'</td><td>'+((o.note||'')+'').replaceAll('<','&lt;')+'</td><td><input id="amountFen-'+o.id+'" type="number" min="1" value="'+o.amount_fen+'" style="width:90px"></td><td><button data-id="'+o.id+'" data-action="approve">确认到账</button><button data-id="'+o.id+'" data-action="reject">驳回</button></td></tr>').join('');el('orderRows').querySelectorAll('button[data-id]').forEach(b=>b.onclick=()=>act(b.dataset.id,b.dataset.action))}async function act(id,a){const body=a==='approve'?JSON.stringify({amountFen:Number(el('amountFen-'+id).value)}):'{}';const r=await fetch('/api/admin/orders/'+id+'/'+a,{method:'POST',headers:{authorization:'Bearer '+auth,'content-type':'application/json'},body});const d=await r.json();el('msg').textContent=d.error||d.message||'完成';loadAll()}function show(which){el('users').classList.toggle('hidden',which!=='users');el('orders').classList.toggle('hidden',which!=='orders')}</script>`.replaceAll("fetch('/api", "fetch('https://107-173-144-109.nip.io:8443/api")

export default { async fetch(request, env) {
  if (request.method === "OPTIONS") return json({ ok: true });
  const url = new URL(request.url); const route = url.pathname;
  try {
    if (route === "/api/health") return json({ ok: true, version: "1.0.3-sites" });
    if (route === "/admin" && request.method === "GET") return new Response(adminPage, { headers: { "content-type": "text/html; charset=utf-8" } });
    if (route === "/api/account/register" && request.method === "POST") {
      const body = await request.json(); const email = text(body.email).toLowerCase(); const password = text(body.password);
      if (!/^\S+@\S+\.\S+$/.test(email) || password.length < 8) return json({ error: "请输入有效邮箱和至少 8 位密码" }, 400);
      const exists = await env.DB.prepare("SELECT id FROM accounts WHERE email=?").bind(email).first(); if (exists) return json({ error: "该账号已存在" }, 409);
      const id = crypto.randomUUID(); const stored = await passwordHash(password); await ensureBalanceColumn(env); await env.DB.batch([env.DB.prepare("INSERT INTO accounts(id,email,password_hash,credits,balance_fen,created_at) VALUES(?,?,?,?,?,?)").bind(id, email, stored, 0, 50, now()), env.DB.prepare("INSERT INTO ledger(id,account_id,type,amount,balance,description,created_at) VALUES(?,?,?,?,?,?,?)").bind(crypto.randomUUID(), id, "grant", 50, 50, "新用户注册赠送", now())]);
      const token = crypto.randomUUID(); await env.DB.prepare("INSERT INTO sessions(token_hash,account_id,expires_at) VALUES(?,?,?)").bind(await sha256(token), id, new Date(Date.now() + 30 * 86400000).toISOString()).run(); return json({ token, user: publicUser({ id, email, balance_fen: 50 }) });
    }
    if (route === "/api/account/login" && request.method === "POST") { const body = await request.json(); await ensureBalanceColumn(env); const account = await env.DB.prepare("SELECT * FROM accounts WHERE email=?").bind(text(body.email).toLowerCase()).first(); if (!account || !(await checkPassword(text(body.password), account.password_hash))) return json({ error: "账号或密码不正确" }, 401); const token = crypto.randomUUID(); await env.DB.prepare("INSERT INTO sessions(token_hash,account_id,expires_at) VALUES(?,?,?)").bind(await sha256(token), account.id, new Date(Date.now() + 30 * 86400000).toISOString()).run(); return json({ token, user: publicUser(account) }); }
    if (route === "/api/account/me" && request.method === "GET") { const user = await requireUser(request, env); return user ? json({ user: publicUser(user) }) : json({ error: "请先登录" }, 401); }
    if (route === "/api/generations" && request.method === "POST") { const user = await requireUser(request, env); if (!user) return json({ error: "请先登录" }, 401); const body = await request.json().catch(() => ({})); const reference = text(body.reference); if (!reference) return json({ error: "缺少生成记录编号" }, 400); await env.DB.prepare("CREATE TABLE IF NOT EXISTS generation_records (id TEXT PRIMARY KEY, account_id TEXT NOT NULL, reference TEXT UNIQUE NOT NULL, duration TEXT NOT NULL, amount_fen INTEGER NOT NULL, content_json TEXT NOT NULL, created_at TEXT NOT NULL)").run(); await env.DB.prepare("INSERT OR IGNORE INTO generation_records(id,account_id,reference,duration,amount_fen,content_json,created_at) VALUES(?,?,?,?,?,?,?)").bind(crypto.randomUUID(), user.id, reference, text(body.duration), Math.floor(Number(body.amountFen || 0)), JSON.stringify(body.content ?? null), now()).run(); return json({ ok: true }); }
    if (route === "/api/generations" && request.method === "GET") { const user = await requireUser(request, env); if (!user) return json({ error: "请先登录" }, 401); await env.DB.prepare("CREATE TABLE IF NOT EXISTS generation_records (id TEXT PRIMARY KEY, account_id TEXT NOT NULL, reference TEXT UNIQUE NOT NULL, duration TEXT NOT NULL, amount_fen INTEGER NOT NULL, content_json TEXT NOT NULL, created_at TEXT NOT NULL)").run(); const rows = await env.DB.prepare("SELECT id,reference,duration,amount_fen,content_json,created_at FROM generation_records WHERE account_id=? ORDER BY created_at DESC LIMIT 100").bind(user.id).all(); return json({ records: (rows.results || []).map((row) => ({ ...row, amountYuan: (Number(row.amount_fen) / 100).toFixed(2), content: JSON.parse(row.content_json || "null") })) }); }
    if ((route === "/api/billing/consume" || route === "/api/billing/refund") && request.method === "POST") { const user = await requireUser(request, env); if (!user) return json({ error: "请先登录" }, 401); const body = await request.json().catch(() => ({})); const amountFen = Math.floor(Number(body.amountFen ?? Number(body.amount || 0) * 10)); const reference = text(body.reference); if (!Number.isFinite(amountFen) || amountFen < 1 || !reference) return json({ error: "余额扣款参数无效" }, 400); await ensureLedgerReference(env); await ensureBalanceColumn(env); const ledgerReference = route.endsWith("/refund") ? `refund:${reference}` : reference; const existing = await env.DB.prepare("SELECT balance FROM ledger WHERE account_id=? AND reference=? LIMIT 1").bind(user.id, ledgerReference).first(); if (existing) { const current = await env.DB.prepare("SELECT id,email,balance_fen FROM accounts WHERE id=?").bind(user.id).first(); return json({ user: publicUser(current), idempotent: true }); } const account = await env.DB.prepare("SELECT id,email,balance_fen FROM accounts WHERE id=?").bind(user.id).first(); const currentFen = Number(account?.balance_fen || 0); if (route.endsWith("/consume")) { if (currentFen < amountFen) return json({ error: "余额不足，请先充值" }, 402); const balanceFen = currentFen - amountFen; await env.DB.batch([env.DB.prepare("UPDATE accounts SET balance_fen=? WHERE id=?").bind(balanceFen, user.id), env.DB.prepare("INSERT INTO ledger(id,account_id,type,amount,balance,description,created_at,reference) VALUES(?,?,?,?,?,?,?,?)").bind(crypto.randomUUID(), user.id, "consume", -amountFen, balanceFen, text(body.description) || "生成脚本", now(), reference)]); return json({ user: publicUser({ ...account, balance_fen: balanceFen }) }); } const balanceFen = currentFen + amountFen; await env.DB.batch([env.DB.prepare("UPDATE accounts SET balance_fen=? WHERE id=?").bind(balanceFen, user.id), env.DB.prepare("INSERT INTO ledger(id,account_id,type,amount,balance,description,created_at,reference) VALUES(?,?,?,?,?,?,?,?)").bind(crypto.randomUUID(), user.id, "refund", amountFen, balanceFen, text(body.description) || "生成失败退款", now(), ledgerReference)]); return json({ user: publicUser({ ...account, balance_fen: balanceFen }) }); }
    if (route === "/api/billing/quote" && request.method === "POST") { const duration = text((await request.json()).duration); const seconds = Number(duration.match(/^\d+/)?.[0] || 0); if (![10, 15, 30, 45, 60].includes(seconds)) return json({ error: "请选择有效的脚本时长" }, 400); const amountFen = Math.ceil(seconds / 5) * 10; return json({ duration, amountFen, amountYuan: (amountFen / 100).toFixed(2) }); }
    if (route === "/api/billing/orders" && request.method === "POST") { const user = await requireUser(request, env); if (!user) return json({ error: "请先登录" }, 401); const body = await request.json(); const provider = text(body.provider); let pack = packages(text(body.packageId)); const amountFen = Number(body.amountFen); if (provider === "alipay_personal" && Number.isInteger(amountFen) && amountFen >= 200) pack = [amountFen, Math.floor(amountFen / 10)]; if (!pack || !["wechat", "alipay_personal", "mock"].includes(provider)) return json({ error: "充值参数无效" }, 400); const packageId = provider === "alipay_personal" && Number.isInteger(amountFen) && amountFen >= 200 ? "custom" : text(body.packageId); const id = `RC${Date.now()}${crypto.randomUUID().slice(0, 8)}`; await env.DB.prepare("INSERT INTO orders(id,account_id,provider,package_id,amount_fen,credits,status,created_at) VALUES(?,?,?,?,?,?,?,?)").bind(id, user.id, provider, packageId, pack[0], pack[1], "pending", now()).run(); const order = { id, userId: user.id, provider, packageId, amountFen: pack[0], credits: pack[1], status: "pending" }; return json({ order, payment: provider === "wechat" ? { mode: "wechat_native_pending", action: `/api/billing/orders/${id}/wechat/native` } : provider === "alipay_personal" ? { mode: "manual", message: "个人收款码需人工审核" } : { mode: "mock" } }, 201); }
    const native = route.match(/^\/api\/billing\/orders\/([^/]+)\/wechat\/native$/); if (native && request.method === "POST") { const user = await requireUser(request, env); if (!user) return json({ error: "请先登录" }, 401); const order = await env.DB.prepare("SELECT * FROM orders WHERE id=? AND account_id=?").bind(native[1], user.id).first(); if (!order || order.provider !== "wechat") return json({ error: "充值订单不存在" }, 404); const codeUrl = await wechatNative(env, order); return json({ order, payment: { mode: "wechat_native", codeUrl } }); }
    const mock = route.match(/^\/api\/billing\/orders\/([^/]+)\/mock-pay$/); if (mock && request.method === "POST") { const user = await requireUser(request, env); if (!user) return json({ error: "请先登录" }, 401); const order = await env.DB.prepare("SELECT * FROM orders WHERE id=? AND account_id=?").bind(mock[1], user.id).first(); if (!order || order.provider !== "mock") return json({ error: "充值订单不存在" }, 404); if (order.status !== "paid") { await env.DB.batch([env.DB.prepare("UPDATE orders SET status='paid',paid_at=? WHERE id=? AND status='pending'").bind(now(), order.id), env.DB.prepare("UPDATE accounts SET balance_fen=COALESCE(balance_fen,credits*10)+? WHERE id=?").bind(order.amount_fen, user.id)]); } const updated = await env.DB.prepare("SELECT id,email,balance_fen,credits FROM accounts WHERE id=?").bind(user.id).first(); return json({ user: publicUser(updated) }); }
    const proof = route.match(/^\/api\/billing\/orders\/([^/]+)\/submit-proof$/); if (proof && request.method === "POST") { const user = await requireUser(request, env); if (!user) return json({ error: "请先登录" }, 401); const body = await request.json().catch(() => ({})); const order = await env.DB.prepare("SELECT * FROM orders WHERE id=? AND account_id=?").bind(proof[1], user.id).first(); if (!order || order.provider !== "alipay_personal") return json({ error: "充值订单不存在" }, 404); await ensureProofTable(env); await env.DB.batch([env.DB.prepare("INSERT OR REPLACE INTO payment_proofs(order_id,note,created_at) VALUES(?,?,?)").bind(order.id, text(body.note), now()), env.DB.prepare("UPDATE orders SET status='proof_submitted' WHERE id=? AND status='pending'").bind(order.id)]); return json({ ok: true, message: "付款备注已提交，管理员确认后会增加积分" }); }
    if (route.match(/^\/api\/admin\/users\/[^/]+\/recharge$/) && request.method === "POST") { if (!(await isAdmin(request, env))) return json({ error: "管理员账户无权限" }, 403); const id = route.split("/")[4]; const body = await request.json().catch(() => ({})); const amountFen = Math.floor(Number(body.amountFen)); if (!Number.isFinite(amountFen) || amountFen < 1) return json({ error: "充值金额必须大于 0" }, 400); await ensureBalanceColumn(env); const account = await env.DB.prepare("SELECT balance_fen,credits FROM accounts WHERE id=?").bind(id).first(); if (!account) return json({ error: "用户不存在" }, 404); const balanceFen = Number(account.balance_fen ?? Number(account.credits || 0) * 10) + amountFen; const ref = `admin:${crypto.randomUUID()}`; await env.DB.batch([env.DB.prepare("UPDATE accounts SET balance_fen=? WHERE id=?").bind(balanceFen,id),env.DB.prepare("INSERT INTO ledger(id,account_id,type,amount,balance,description,created_at,reference) VALUES(?,?,?,?,?,?,?,?)").bind(crypto.randomUUID(),id,"recharge",amountFen,balanceFen,"管理员手工充值",now(),ref)]); return json({ message: `已为用户充值 ¥${(amountFen/100).toFixed(2)}` }); }
    if (route.match(/^\/api\/admin\/users\/[^/]+$/) && request.method === "PATCH") { if (!(await isAdmin(request, env))) return json({ error: "管理员账户无权限" }, 403); const id = route.split("/")[4]; const body = await request.json().catch(() => ({})); await ensureAccountAdminColumns(env); await env.DB.prepare("UPDATE accounts SET admin_note=?,membership=? WHERE id=?").bind(text(body.note),text(body.membership)||"普通用户",id).run(); return json({ ok:true }); }
    if (route === "/api/admin/users" && request.method === "GET") { if (!(await isAdmin(request, env))) return json({ error: "管理员账户无权限" }, 403); await ensureBalanceColumn(env); await ensureAccountAdminColumns(env); await env.DB.prepare("CREATE TABLE IF NOT EXISTS generation_records (id TEXT PRIMARY KEY, account_id TEXT NOT NULL, reference TEXT UNIQUE NOT NULL, duration TEXT NOT NULL, amount_fen INTEGER NOT NULL, content_json TEXT NOT NULL, created_at TEXT NOT NULL)").run(); const rows = await env.DB.prepare("SELECT a.id,a.email,a.balance_fen,a.created_at,a.admin_note,a.membership,COUNT(g.id) AS generation_count FROM accounts a LEFT JOIN generation_records g ON g.account_id=a.id GROUP BY a.id ORDER BY a.created_at DESC LIMIT 500").all(); return json({ users: (rows.results || []).map((row) => ({ ...row, balance_yuan: (Number(row.balance_fen || 0) / 100).toFixed(2), generation_count: Number(row.generation_count || 0) })) }); }
    if (route === "/api/admin/orders/history" && request.method === "GET") { if (!(await isAdmin(request, env))) return json({ error: "管理员账户无权限" }, 403); await ensureProofTable(env); const rows = await env.DB.prepare("SELECT o.id,o.account_id,o.provider,o.amount_fen,o.status,o.created_at,o.paid_at,a.email,p.note FROM orders o JOIN accounts a ON a.id=o.account_id LEFT JOIN payment_proofs p ON p.order_id=o.id ORDER BY o.created_at DESC LIMIT 500").all(); const ledgerRows = await env.DB.prepare("SELECT l.created_at,l.amount AS amount_fen,a.email,'admin' AS provider,'manual' AS status,l.description FROM ledger l JOIN accounts a ON a.id=l.account_id WHERE l.type='recharge' AND l.description='管理员手工充值' ORDER BY l.created_at DESC LIMIT 500").all(); return json({ orders: [...(rows.results || []), ...(ledgerRows.results || [])].sort((a,b) => String(b.created_at).localeCompare(String(a.created_at))) }); }
    if (route === "/api/admin/orders" && request.method === "GET") { if (!(await isAdmin(request, env))) return json({ error: "管理员账户无权限" }, 403); await ensureProofTable(env); const rows = await env.DB.prepare("SELECT o.*, a.email, p.note FROM orders o JOIN accounts a ON a.id=o.account_id LEFT JOIN payment_proofs p ON p.order_id=o.id WHERE o.status='proof_submitted' ORDER BY o.created_at ASC").all(); return json({ orders: rows.results || [] }); }
    const adminAction = route.match(/^\/api\/admin\/orders\/([^/]+)\/(approve|reject)$/); if (adminAction && request.method === "POST") { if (!(await isAdmin(request, env))) return json({ error: "管理员账户无权限" }, 403); const order = await env.DB.prepare("SELECT * FROM orders WHERE id=?").bind(adminAction[1]).first(); if (!order || order.status !== "proof_submitted") return json({ error: "订单不存在或已处理" }, 404); if (adminAction[2] === "reject") { await env.DB.prepare("UPDATE orders SET status='rejected' WHERE id=?").bind(order.id).run(); return json({ message: "订单已驳回" }); } const body = await request.json().catch(() => ({})); const amountFen = Math.floor(Number(body.amountFen ?? order.amount_fen)); if (!Number.isFinite(amountFen) || amountFen < 1 || amountFen > 100000000) return json({ error: "发放金额必须有效" }, 400); await ensureBalanceColumn(env); const account = await env.DB.prepare("SELECT balance_fen,credits FROM accounts WHERE id=?").bind(order.account_id).first(); const balanceFen = Number(account?.balance_fen ?? Number(account?.credits || 0) * 10) + amountFen; await env.DB.batch([env.DB.prepare("UPDATE orders SET status='paid',paid_at=? WHERE id=? AND status='proof_submitted'").bind(now(), order.id), env.DB.prepare("UPDATE accounts SET balance_fen=? WHERE id=?").bind(balanceFen, order.account_id), env.DB.prepare("INSERT INTO ledger(id,account_id,type,amount,balance,description,created_at) VALUES(?,?,?,?,?,?,?)").bind(crypto.randomUUID(), order.account_id, "recharge", amountFen, balanceFen, `人工审核到账：${order.package_id}`, now())]); return json({ message: `已到账 ¥${(amountFen / 100).toFixed(2)}`, balanceFen }); }
    if (route === "/api/billing/webhook/wechat" && request.method === "POST") { const raw = await request.text(); const body = JSON.parse(raw); if (!env.WECHAT_API_V3_KEY || !body.resource) return json({ code: "FAIL", message: "回调配置不完整" }, 400); if (!(await verifyWechat(request, raw, env))) return json({ code: "FAIL", message: "回调验签失败" }, 401); const result = await decryptWechat(body.resource, env.WECHAT_API_V3_KEY); if (result.trade_state !== "SUCCESS") return json({ code: "SUCCESS", message: "成功" }); const order = await env.DB.prepare("SELECT * FROM orders WHERE id=?").bind(result.out_trade_no).first(); if (!order || order.provider !== "wechat" || Number(result.amount?.total) !== Number(order.amount_fen)) return json({ code: "FAIL", message: "订单校验失败" }, 400); if (order.status !== "paid") { await env.DB.batch([env.DB.prepare("UPDATE orders SET status='paid',paid_at=? WHERE id=? AND status='pending'").bind(now(), order.id), env.DB.prepare("UPDATE accounts SET balance_fen=COALESCE(balance_fen,credits*10)+? WHERE id=?").bind(order.amount_fen, order.account_id)]); } return json({ code: "SUCCESS", message: "成功" }); }
    return json({ error: "Not found" }, 404);
  } catch (error) { return json({ error: error?.message || "服务器错误" }, error?.status || 500); }
} };
